# This is a basic workflow to help you get started with Actions

name: Linttttttttttttttttt 

# Controls when the action will run. 
on:
  pull_request:
# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      - uses: actions/checkout@v2
      - name: dgsdgsdgsdgsd
        run: echo ${{github.run_id}}
        # run: curl --location --request POST 'https://api.github.com/repos/k-lusine/next-intl/actions/workflows/submodules.yml/dispatches' --header "Accept:application/vnd.github.v3+json" --header 'Content-Type:application/json' --user 'k-lusine:${{secrets.PAT_TOKEN}}' --data-raw '{"ref":"main"}'
